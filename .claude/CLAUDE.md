# dream-forge Project Instructions

## Experiment Capture Reminder

After completing a task and committing code, ask the user if they want to capture the work as a training experiment by running `/dream-forge`. Every resolved task (bug fix, feature, research finding) is potential training data for the self-improving model. Don't skip this — the training pipeline needs real experiments to function.
